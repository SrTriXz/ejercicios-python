# completa el ejercicio
def dividir(a, b):
    try:
        return a/b
            
        
    except:

        if b == 0:
            return "losient0, no se puede divider entre cero"


print(dividir(5,2))
        



